# Configs

- `labels.jsonl`：从老师 Excel 导出的规范化 458 Label；ID 始终按字符串保存。
- `labels.report.json`：导出时的条数、空字段、重复项和特殊类别计数。

`labels.jsonl` 每行字段为 `label_path`、`label_id`、`label_name`、`label_type`、`definition`、`core_concepts`、`common_assessments`、`distinctions`。

